server:
  port: 1001

eureka:
  instance:
    hostname: 127.0.0.1
  client:
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:8761/eureka/

spring:
  application:
    name: user-service

swagger:
  title: "user-service"
  description: "用户接口"
  version: V1.0
  enabled: true
