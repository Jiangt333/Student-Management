server:
  port: 1001

spring:
  application:
    name: user-service
  profiles:
    active: ${SPRING_PROFILE_ACTIVE:dev}

swagger:
  title: "user-service"
  description: "用户接口"
  version: V1.0
  enabled: true
