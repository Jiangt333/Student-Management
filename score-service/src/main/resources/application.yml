server:
  port: 1002

eureka:
  instance:
    hostname: 127.0.0.1
  client:
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:8761/eureka/

spring:
  application:
    name: score-service

swagger:
  title: "score-service"
  description: "成绩接口"
  version: V1.0
  enabled: true
